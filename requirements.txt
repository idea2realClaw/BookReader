flet>=0.85.0
# flet_audio：Flet 官方音频控件（Flutter audioplayers），安卓端 TTS 应用内播放用，
# 原生支持 Android，无 C 扩展依赖。
# 注意：Python 端通过本文件安装；dart 端（AudioService 原生控件）必须在
# pyproject.toml 的 [tool.flet.flutter.dependencies] 里声明 flet_audio = "*"，
# flet build apk 才会把 dart 包打进 APK。两端缺一不可：
# - 只有 Python 端：import 成功但 service 注册失败 → play() 超时 → 听不到声音
# - 只有 dart 端：import 失败 → 走 am start 回退 → 系统播放器不自动播放
flet_audio>=0.85.0
pypdf>=6.0.0
gtts>=2.5.0
requests>=2.31.0
# Edge TTS 统一后端：ui/edge_tts_lite 仅依赖 websockets + certifi（均为纯 Python，
# 可在 Flet 安卓打包环境安装），复刻 edge_tts 协议，使安卓端也能用男/女声 + 倍速。
# 注意：官方 edge_tts / pygame 不列入（其 C 扩展 wheel 在安卓缺失，会导致 flet build apk 失败）；
# 桌面端 pygame 应用内播放由本地虚拟环境（pip install pygame）提供。
websockets>=12.0
certifi>=2024.0.0
